const PATTERN_FLAGS = {
    PASS: 'PASS'
};

const BASE_NON_EMPTY_TYPES  = ['string', 'boolean', 'number'];
const BASE_TYPES            = ['string', 'boolean', 'number', 'undefined'];
const ARG_TYPES             = { simple: 'simple', multiple: 'multiple' };

module.exports = {
    ARG_TYPES,
    BASE_TYPES,
    PATTERN_FLAGS,
    BASE_NON_EMPTY_TYPES,
};
